# AGENTS.md

> This file has been superseded by the Orion-OS v2.4 framework.
> The canonical AGENTS.md now lives at `.agents/AGENTS.md`.
> Project-specific extensions live at `.agents/AGENTS.project.md`.
>
> The `.agents/` directory is in `.gitignore` — it travels with the human's local machine, not the repository.
>
> If you're an AI agent starting a session, read `.agents/AGENTS.md` first, then `.agents/AGENTS.project.md`.
> If those files don't exist, this project hasn't been initialized with Orion-OS v2.4 yet.
